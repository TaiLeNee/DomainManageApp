package view;

import javax.swing.*;
import java.awt.*;

public class DomainManagementPanel extends JPanel {
    public DomainManagementPanel() {
        setBackground(Color.LIGHT_GRAY);
        add(new JLabel("Nội dung Panel: Quản lý tên miền"));
        // Thêm các thành phần UI khác cho chức năng này ở đây
    }
}
