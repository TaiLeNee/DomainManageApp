package view;

import javax.swing.*;
import java.awt.*;

public class DomainExtensionPanel extends JPanel {
    public DomainExtensionPanel() {
        setBackground(Color.WHITE); // Màu nền khác nhau để dễ phân biệt
        add(new JLabel("Nội dung Panel: Phần mở rộng tên miền"));
        // Thêm các thành phần UI khác cho chức năng này ở đây
    }
}
